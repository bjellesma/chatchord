{
  "name": "chatcord",
  "version": "1.0.0",
  "description": "Realtime chat app with rooms",
  "main": "server.py",
  "scripts": {
    "server": "pipenv run python server.py",
    "dump-requirements": "pipenv lock --requirements > requirements.txt",
    "tc-watch": "tsc static/js/main.ts -w",
    "heroku": "pipenv run heroku local",
    "logs": "sudo heroku logs --tail"
  },
  "author": "Brad Traversy",
  "license": "MIT",
  "dependencies": {
    "moment": "^2.29.2",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}
